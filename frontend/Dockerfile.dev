FROM node:20-alpine

WORKDIR /app

COPY package*.json ./

# Do NOT run `npm install` during build
# Instead, we'll run it during startup

COPY . .

EXPOSE 8080

# Install dependencies on container start, then run dev
CMD ["sh", "-c", "npm install && npm run dev"]